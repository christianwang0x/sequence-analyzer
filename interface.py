ENCODING_SCHEMES = (base64, hexidecimal, binary, octal)

EncodingScheme()
  .encode(data)
  .decode(data)
  .charset = ()
  

Column(sequence, index)
  .bytes []
  .frequencies {}
  .entry_count int
  .poly_reg(max_order)
    returns poly, correlation

